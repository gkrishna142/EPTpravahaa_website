

version: "3.9"

services:
  web:
    image: jayakrishnagolla/ept-dashboard:latest
    container_name: eptpravahaa_web
    ports:
      - "3000:3000"
    restart: always
    environment:
      - NODE_ENV=production
